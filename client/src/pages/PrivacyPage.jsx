import StaticInfoPage from "./StaticInfoPage";

export default function PrivacyPage() {
    return (
        <StaticInfoPage title="Privacy policy">
            <p>
                TradeX is built for the campus community. Your account is tied to your university email.
                We store the data needed to run listings, messages, and transactions (see the app’s
                database). Replace this text with a full policy before a public launch.
            </p>
        </StaticInfoPage>
    );
}
